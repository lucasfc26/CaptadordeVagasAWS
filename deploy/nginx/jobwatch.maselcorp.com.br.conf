# Host nginx (Ubuntu) — not the JobWatch container.
# Install as /etc/nginx/sites-available/jobwatch.maselcorp.com.br
# then: ln -s .../sites-available/... .../sites-enabled/ && nginx -t && systemctl reload nginx
#
# After HTTP works: certbot --nginx -d jobwatch.maselcorp.com.br

server {
    listen 80;
    listen [::]:80;
    server_name jobwatch.maselcorp.com.br;

    client_max_body_size 2m;

    location / {
        proxy_pass http://127.0.0.1:18083;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}
